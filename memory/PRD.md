# GigLine Safety & Compliance — PRD

## Original Problem Statement
B2B lead-generation funnel for an OSHA compliance consultancy (GigLine, Vince Lawrence). React + FastAPI + MongoDB. Strict factual accuracy on case studies, pixel-matched prototypes, industry-specific SEO copy, real-world photography, source-attribution tracking. SEO schemas in JSON-LD must stay synced between React components and the static pre-render script (`generate-seo-pages.js`).

## Personas
- HR Manager / Safety Coordinator (NC Piedmont Triad mfg + warehouse)
- Owner-operator of small manufacturing shop (gets cited, needs help fast)
- Supervisor on the floor (the person who actually does safety)

## Core Stack
React 18 (CRA), Tailwind, FastAPI, MongoDB (motor), Stripe LIVE, Resend LIVE, MailerLite LIVE. Static SSR HTML pre-render via `frontend/scripts/generate-seo-pages.js`.

## Implemented (current state — Feb 2026)
- Mature funnel: home, services, intake, walkthrough, Safety Check (tiered results), Field Notes (25), Case Study, About, Contact.
- Supervisor Safety Starter System paid checkout (LIVE Stripe + Resend auto-PDF delivery + thank-you page polling).
- Gated lead magnets w/ auto-open PDF + email delivery: Heat Guide, Sample Report (real redacted 18-page PDF), OSHA Inspection Guide (HR-targeted).
- `/resources` hub w/ 8 tiles (OSHA Compliance Guide [pillar], Safety Check, Heat Guide, HazCom, Sample Report, OSHA Inspection Guide, Supervisor Safety Starter System, Field Checklist).
- Admin Dashboard `/api/admin/stats` tiles: safety checks, risk breakdown, walkthroughs, downloads, heat guide leads, quick contacts, sample reports, **hr_osha_guide** [Feb 2026].
- Quick-Contact micro-form on intake with attribution tracking.
- Theme-aware favicons, auto-OG image generation, JSON-LD schema sync in `generate-seo-pages.js`.
- Footer + global Google Review link w/ UTM params.
- Legacy `$650` `/onboarding` retired — all walkthrough copy cites "From $1,200".
- Factual-accuracy sweep done (no Amero Steel / BF Goodrich employment claims).
- Backlink playbook in `/app/memory/backlink-playbook.md`.
- **GL-WEB-024 (Feb 2026)**: `/contact` now has a 4-field Simple Contact Form (name/email/phone/message + honeypot) wired to `/api/contact-message/submit` — sends Resend confirmation to prospect + notification to Vince. Below the form, a navy/gold Calendly Scope Call CTA block ("Prefer a scheduled call?" → "Book a Scope Call") using a **placeholder Calendly URL** — swap `CALENDLY_SCOPE_CALL_URL` in `/app/frontend/src/components/ScopeCallCTA.js` once Vince completes GL-WEB-022 Calendly Stage 1 setup. Full intake form link preserved as third option.
- **Services Comparison Table (Feb 2026)**: `/services#compare` — new side-by-side comparison of the four core engagements (Safety Walkthrough / Documentation Review / Compliance Readiness Visit / OSHA-Ready Control System). 12 rows (on-site walkthrough, photo findings, CFR citations, program review, compliance %, RED/AMBER/GREEN, 90-day tracker, physical binder buildout, supervisor handoff, 30-day check-in, turnaround, best-when). CRV column highlighted with "★ Most Requested" gold badge. Mobile-friendly via `overflow-x-auto` with `min-w-[880px]`. Sticky jump-nav now includes "Compare" link (`#compare`). Rows fire `compare_row_click` GA4 event with `row_label`.
- **GL-WEB-017 (Jul 2026)**: Standalone redacted Corrective Action Log — `/sample-corrective-action-log` serves an ungated 12-page PDF (branded cover + pages 7–17 excerpt from the full sample report). Extraction script: `/app/scripts/extract_corrective_action_log.py`. Static asset: `/app/frontend/public/assets/GigLine_Sample_Corrective_Action_Log.pdf`. Routing: vercel.json rewrite for prod + `/app/frontend/src/setupProxy.js` for dev preview. Secondary-style download button (gold border / navy text / white bg) added on `/case-study/metals-fabrication-statesville` immediately below the "After the report" paragraph, with "No email required. Opens as PDF." subtext. Full `/sample-report` gated flow unchanged.
- **New flat logo (Jul 2026)**: Swapped canonical `gigline-logo-3d.png` + `gigline-logo-dark-bg.png` to the new flat 2D "GigLine" wordmark with yellow belt bars + charcoal buckle icon. Transparent margins auto-cropped so the logo tight-fits its container. Dark-bg variant auto-generated (charcoal → white, yellow preserved). Cache-buster bumped `?v=6 → ?v=7` across 8 files. Navbar row heights removed (was `h-20 md:h-24…`) in favor of `py-1 lg:py-1.5 xl:py-2` so header naturally sizes to logo. Logo sizes: `h-12 md:h-16 lg:h-20 xl:h-24 2xl:h-28`.
- **Cover v3 standard (Jul 2026)**: Reusable branded PDF cover template locked in at `/app/backend/lib/pdf_cover.py` (navy `#102A43` bg, new flat logo, gold `#C9A84C` accents, "What's Inside" 2-col checklist, optional process-flow strip, footer strap). Public API: `build_cover(...)` for standalone covers, `prepend_cover_to_pdf(src, dst, ..., replace_page_1=False)` for wrapping existing PDFs. Applied to all 8 collateral PDFs via `/app/scripts/apply_cover_v3_to_all.py`: Field Manual, FAQ, Pricing, OSHA Inspection Guide, Statesville Case Study, Doc Review Prep, Heat Guide, and Corrective Action Log excerpt. Favicons + OG image also refreshed via `/app/scripts/propagate_new_logo.py`. Deprecated `#1C2B2B` dark-green now documented as forbidden in `pdf_cover.py`.
- **GL-WEB-018f (Jul 2026) — Personalized client covers**: `build_cover()` and `prepend_cover_to_pdf()` now accept an optional `client_name` kwarg — when set, the cover header slug reads `PREPARED FOR {NAME} | {YEAR}`. Per-PDF cover metadata moved to `/app/backend/lib/pdf_cover_metadata.py` so both the batch script AND the admin endpoint share one source of truth. New admin endpoints: `GET /api/admin/personalized-pdfs` (whitelist) and `GET /api/admin/personalized-pdf/{filename}?client_name=...&token=...` (streams in-memory personalized PDF; nothing written to `/assets`; path-traversal guarded via basename + whitelist). New admin UI tab **"Personalize PDF"** with dropdown + client-name input + live preview + download button.
- **GL-WEB-018 (Jul 2026)** — Supervisor Safety Starter System soft-disabled via feature flag. Backend `SUPERVISOR_KIT_ENABLED=false` in `/app/backend/.env` (checkout endpoints return 503; /verify still works for in-flight paid orders). Frontend `REACT_APP_SUPERVISOR_KIT_ENABLED=false` in `/app/frontend/.env` (shared helper: `/app/frontend/src/config/features.js`). Gated across: `/supervisor-kit` (redirects to `/services`), Homepage kit band, Footer link, Resources hub tile, Services standalone product card, 3 blog cross-sell blocks (Forklift / HazCom / OSHA 300 Log), Field Notes `kitCrossSell` block, and Admin `Kit PDFs` tab. Admin still has direct API access to the 11 PDFs (`/api/admin/kit-files`). Re-enable = flip both env flags to `true` and rebuild.
- **HazCom Starter Pack "Buy Now" contrast fix (Jul 2026)**: `/hazcom-starter-pack` CTA button was rendering `bg-[#102A43] text-[#1C2B2B]` (navy-on-navy with dark text) — completely invisible. Fixed to the standard gold-on-navy CTA pattern (`bg-[#C9A84C] text-[#102A43]`) used across the rest of the site. Full contrast sweep run across all pages/components; no other invisible buttons found.
- **Footer local-SEO tag (Jul 2026)**: Copyright line now includes `· Piedmont Triad, NC` after "All rights reserved" (`/app/frontend/src/components/Footer.js`).
- **Admin Kit Orders view (Jul 2026)**: New `/admin` tab **Kit Orders** unifying paid orders from `gl_citation_proof_kit_orders` + `gl_supervisor_kit_orders`. Backend: `GET /api/admin/kit-orders?token=…&filter=all|needs_shipping|auto_delivered|pending_manual|shipped` returns normalized orders + counts; `POST /api/admin/kit-orders/{session_id}/mark-shipped` accepts `{token, tracking_number, carrier}` to flip status → `shipped` on either collection. Frontend: `KitOrdersTab` in `AdminPage.js` — 4-tile count strip (Total Paid / Needs Shipping [orange callout] / Citation-Proof / Supervisor Kit), 5 filter chips, sortable table (Paid date, Product, Tier, Amount, Customer, Shipping addr, Status, Action), and a Mark-Shipped modal with carrier dropdown + optional tracking number. 15 pytest cases in `/app/backend/tests/test_admin_kit_orders.py` cover auth, filter validation, response shape, physical-only filtering, and shipped mutations.
- **Uniform SKU metadata across all 9 checkout SKUs (Jul 2026)**: Added explicit `sku: "hazcom-starter-pack"` to `HAZCOM_PRODUCT` in `config.py` + passed it into the Stripe session metadata in `routes/hazcom.py`. Now every one of the 9 checkout endpoints (6 citation-proof kits + 2 supervisor kit variants + hazcom starter) writes a `metadata["sku"]` field on the Session, so a single `stripe.checkout.Session.retrieve()` call is sufficient to identify the exact product purchased — no line-items fetch required.
- **Resend My Kit — self-serve email recovery (Jul 2026)**: New public page `/resend-my-kit` for buyers whose kit-delivery emails hit spam. Backend endpoint `POST /api/kit-orders/resend { email }` searches all 3 order collections (citation-proof-kit + supervisor-kit + hazcom via `payment_transactions`) for paid orders matching the email, then re-invokes the same delivery functions (`_send_buyer_confirmation`, `_send_kit_buyer_email`, `_send_hazcom_delivery_email`) that fired on original purchase. Rate-limited to 3 attempts / email / rolling 60-min window (returns 429 with fallback CTA to Vince's email). Every attempt logged to `gl_resend_log` collection for audit. Response is always generic ("if a paid order is on file, we've re-sent it") so email enumeration is impossible. **Zero-match alert:** when a submitted email has no paid order on file, Vince gets a lightweight Resend heads-up email (deduped to 1 alert / email / hour) with the attempted email + client IP + user-agent — early warning for chargebacks, mistyped checkout emails, or bot probes. Frontend: `ResendMyKitPage.js` with on-brand navy hero + form + 4 status states (idle/loading/success/rate_limited/error). Linked from Footer ("Resend My Kit") and inline on the Citation-Proof Kit thank-you page's "didn't get the email?" paragraph. Page is `noindex, follow`. 6 pytest cases in `test_kit_resend.py`.
- **Kit Lifecycle Emails — Shipped confirmation + 90-day anniversary follow-up (Jul 2026)**: Two new post-purchase touchpoints wired into the same order objects. **Shipped confirmation**: fires the moment Vince taps *Mark Shipped* in `/admin` → Resend email to the buyer with the carrier + tracking number + carrier deep-link (USPS / UPS / FedEx / DHL supported; "Other" gracefully skips the link). Wired directly into `admin_mark_kit_shipped` handler as a best-effort dispatch — Resend outage never blocks the DB mutation, response includes `email_sent: true|false` so the admin UI can react. **90-day anniversary follow-up**: new `anniversary_followup_scheduler` in `server.py` runs once per calendar day (>= 14:00 UTC / ~9 AM EST). Uses ±3-day window (87–93 days) so weekend downtime never skips anyone. Idempotent via a write-once `anniversary_followup_sent_at` field — no duplicate emails on scheduler restarts. Fires for BOTH digital and physical buyers: physical uses `shipped_at`, digital uses `paid_at` + `fulfillment_status="auto_delivered"`. Email body asks the buyer if the paperwork survived an inspection since, with a soft Google-review CTA. Module: `/app/backend/lib/kit_lifecycle_emails.py`. 13 pytest cases in `test_kit_lifecycle_emails.py` cover the tracking-URL builder (5 carriers), window symmetry, and boundary conditions. End-to-end verified with a real GigLine-domain address — email delivered successfully to `vince@giglinecompliance.com`.
- **HazCom Pro Kit — Live checkout + full landing page (Aug 2026)**: `hazcom-pro-kit` flipped from `ready:false` (waitlist) → `ready:true` (fully purchasable) across all 3 tiers ($150 Digital / $300 Control System / $600 Binder). **6 new premium marketing images** placed at `/app/frontend/public/assets/kits/hazcom-pro/` — `hazcom-hero.png` (navy hero with 3D kit mockup), `hazcom-whats-inside.png`, `hazcom-delivery.png` (3-tier "How Delivery Works" panel), `hazcom-digital-150.png`, `hazcom-control-300.png`, `hazcom-binder-600.png`. **3 square Stripe thumbnails** auto-generated from the tier images via Python/Pillow letterbox onto 1024x1024 navy canvas (`*-sq.png`) so full tier design renders in Stripe checkout without letterboxing. **Data changes** in `citationProofKits.js`: catalog gets `cardImage: '/assets/kits/hazcom-pro/hazcom-hero.png'`, `ready: true`; `KIT_DETAILS['hazcom-pro-kit']` gets a `productImages` block with `hero`, `whatsInside`, `physicalMockup` (binder-600), `deliverySection` (new field — hazcom-delivery.png), `deliverySectionAlt`, and `tierCards` object. **Backend config** (`config.py`): 3 new `CITATION_PROOF_KIT_PRODUCTS` entries with SKUs `citation-proof-hazcom-pro-digital/control-system/binder`, PDF paths pointing at future files (`GigLine_HazCom_Pro_Digital_Compliance_Kit_150.pdf` / `GigLine_HazCom_Pro_Compliance_Control_System_300.pdf`), image_paths pointing to the square Stripe thumbnails. Missing PDFs trigger the existing graceful fallback (buyer gets "delivery coming separately" email; Vince gets amber ACTION REQUIRED notification) — no code path breaks. **Frontend wiring**: `KitPricingTiers.STRIPE_ENABLED_SLUGS` now includes `'hazcom-pro-kit'` → all 3 tier CTAs render as `<button>` with live checkout instead of `<Link>` to `/contact`. **New "How Delivery Works" section** inserted in `CitationProofKitDetailPage.js` immediately before `#pricing` — renders full-width navy image band when `productImages.deliverySection` is present (only HazCom Pro uses it currently). **Testing**: All 3 tier Stripe sessions verified live (`cs_live_a1XiEb8xdFRGx2yziVURbVwm8lG5IBxpF01P9cctCI6LIJbqsMPhcPXp3b` digital / `...NkTyGZ84w5LWN` control-system / `...NYHtnSs` binder). 67 of 69 kit-related pytests pass — 2 pre-existing failures are `STRIPE_API_KEY` env placeholder issues unrelated to this change. Test updates: `test_unsupported_kit_returns_400` now uses `does-not-exist-kit` slug (hazcom-pro-kit is now supported); `test_list_citation_proof_dedupes_binder_and_control_system` expects 6 unique PDFs (was 4, now LOTO+PIT+HazComPro × 2 each).
- **SEO push: Citation Cost Calculator SSR + NC OSHA Penalty blog + HazCom Pro Kit stakes (Aug 2026)**: (1) **Critical SSR fix** — `/citation-cost-calculator` was in sitemap but had no SSR route, so Google was crawling it and receiving homepage HTML with canonical pointing to `/` (soft-404 signal). Added a full route to `generate-seo-pages.js` with keyword-rich body (~900 words), 2026 penalty schedule breakdown, per-instance multiplier explainer, top-10 most-cited standards with internal Field Notes links, `WebApplication` schema (rich-result eligible for interactive tools) + `FAQPage` schema (4 penalty Q&As, FAQ rich-result eligible) + breadcrumb. Targets high-intent commercial queries: "OSHA penalty calculator", "OSHA fine calculator", "OSHA citation cost estimator". (2) **New blog post** — `/blog/osha-penalty-north-carolina-2026` ("How Much Is an OSHA Violation in North Carolina in 2026?") — 1500 words, 5 penalty tier cards (Serious / Other-Than-Serious / Willful / Repeat / Failure-to-Abate), per-instance multiplier section with worked example, gravity/size/good-faith/history adjustment factors explainer, NC-specific differences (10-day informal conference, 1-800-625-2267 hotline, public-sector coverage), 3 penalty-reduction tactics, calculator CTA + CRV CTA + legal disclaimer. React page: `BlogOSHAPenaltyNC2026.js`. Route registered in `App.js` (`/blog/osha-penalty-north-carolina-2026`). SSR entry added to `generate-seo-pages.js` with Article + Breadcrumb + FAQPage schema (4 NC-specific Q&As). Sitemap updated (priority 0.90). Featured post on `/field-notes` (bumps prior featured to position 3). (3) **HazCom Pro Kit stakes enrichment** — added `stakes` block to `KIT_DETAILS['hazcom-pro-kit']` in `citationProofKits.js` (matches LOTO/PIT depth): kicker "What's at Stake in 2026", headline "OSHA's #1 most-cited standard in general industry", body citing HazCom's decade-long #1 Top 10 rank, 3 stat cards ($16,550 / $165,514 / #1), source line citing 2026 penalty schedule + FY2025 Top 10. Also added 4 `controlMechanisms` (Chemical Control Index™, SDS Gap Severity Grid™, Secondary Container Label Audit, HazCom Training Certification). Instantly upgrades both React page rendering (navy stakes strip now visible on HazCom Pro) AND SSR output (`renderKitDetailRoute` auto-consumes `stakes` + `controlMechanisms`) — no template changes needed. Bumps `/citation-proof-kits/hazcom-pro-kit` SSR HTML from ~16KB to est. 22KB with keyword density on "HazCom citation", "SDS gap", "secondary container labeling", "1910.1200".

## Roadmap (prioritized)
### P0 — none open
### P1
- **HazCom Pro, Incident, and New Hire Orientation kit page deepening**: Baseline copy still needed for these 3 kits following the approved architecture flow lines (same treatment PIT just got, but based on the "approved architecture" rule — no DOCX required per Vince). Sections to fill: `stakes`, richer `outcomes`, `controlMechanisms`, `keyProof`, `faq`. Currently they render the outcome-first hero + tier grid + baseline FAQ but lack the stakes strip and control-mechanism callouts. All 3 are `ready: false` so their tier CTAs still route to `/contact` as "Notify Me When Available" — no Stripe wiring needed yet.
- **Deepen PIT $300 Control System + $600 Binder detail copy**: The uploaded `GigLine_PIT_Compliance_Control_System_300_600.docx` was not parsed in this pass. Extract and feed the $300 and $600 tier-specific value copy into either `KIT_TIERS` (universal) or a per-kit override so the PIT page's Control System / Binder tiers speak the same specific language ("30-Day PIT Records Reset Plan™", "First-Pull Packet™", QR Evidence Hub Setup add-on) as the DOCX.
- **SSR mirror of the 6 new Kit Series pages**: Add `/citation-proof-kits`, `/citation-proof-kits/:slug` (all 5 kits), and `/citation-proof-kits/:slug/thank-you` (noindex) routes to `generate-seo-pages.js`.
- **Field Notes hub audit** (`/field-notes`): Verify all 25 note cards render correctly with hero images (regression check post-refactor).
- **Post-Calendly-setup swap**: Once Vince finishes GL-WEB-022 (Calendly Stage 1 event type), replace placeholder URL in `/app/frontend/src/components/ScopeCallCTA.js` (`CALENDLY_SCOPE_CALL_URL`) with the live Stage 1 link.
- **GL-WEB-026**: Field Notes SSR fix — convert `/field-notes/[slug]` to static generation via the existing `generate-seo-pages.js` pipeline so article body content is present in initial HTML (currently CSR-only per audit).
- Generate 4 remaining collateral PDFs (awaiting names from Vince).
- Fix `gl-fm-2026.pdf` deprecated shield logo (PyMuPDF smask blocker — try pikepdf).
### P2
- Wire real hero photos to remaining 16 Field Notes (low priority per user, waiting on uploads).
- Refactor repeated stats querying in `admin.py` into `get_stats_for_collection(db_collection)` helper.
- Quick-Contact 30-day trendline tile on Admin Dashboard (mini chart per lead magnet).
- "Featured In" logo strip on `/about` when first guest article publishes.
- 4-touch MailerLite retention sequence (Vince user-side build in MailerLite UI).
- MailerLite automation for `hr-osha-guide-download` group (Vince user-side).
- (Optional, future) `aggregateRating` schema enrichment — add once Vince accumulates 5+ verified Google reviews. Currently skipped (Google requires real data, not placeholder).

## Architecture Notes
- All backend routes under `/api`.
- All env via `.env` (REACT_APP_BACKEND_URL, MONGO_URL, ADMIN_PASSWORD=`gigline2026`, Stripe/Resend/MailerLite keys).
- Mongo collections: `walkthrough_requests`, `gl_intake_submissions`, `quick_contacts`, `sample_report_leads`, `osha_inspection_guide_leads`, `heat_guide_leads`, `safety_checks`, plus downloads tracking.
- Admin auth: simple password via `ADMIN_PASSWORD` env (`gigline2026`). `/api/admin/stats?token=...`.
- LIVE Stripe in `.env` — never run real card tests.

## Critical Conventions
- `generate-seo-pages.js` must mirror every content/page change for SSR + SEO.
- All gated downloads: dual-delivery (Resend email + MailerLite group + auto-open PDF tab via `window.open(download_url)`).
- No legacy `$650` pricing anywhere — always "From $1,200" for walkthroughs.
- `data-testid` on every interactive element and critical UI element.

## Recent Changelog
- 2026-08 — **Full site refactor — Find/Build/Maintain operating model + pricing rebase**: Owner-approved refactor implemented in 7 phases. **Pricing rebase**: Safety Walkthrough $1,200 → **$1,300**; Documentation Readiness Review $1,300 → **$1,700**; Compliance Readiness Visit $2,000 → **$2,500**; OSHA-Ready Control System stays $4,500; Quarterly $950/qtr and Annual $12,000/yr unchanged; kit prices ($150/$300/$600) unchanged (Section 26 exception — kit prices remain owned by `citationProofKits.js` to avoid backend/SSR/Stripe circular dependency). **New shared pricing source** at `/app/frontend/src/data/servicePricing.js` — canonical constants + approved savings language + baseline scope. **New service** — Corrective Action Implementation page live at `/services/corrective-action-implementation` (custom quote, from $2,500). **Homepage** — primary hero CTA switched to "Request a Compliance Readiness Visit" (was "Request a Walkthrough"); new FLOOR → FINDINGS → FIXES → PROOF section inserted after hero on navy background; top-3 diagnostic cards now Walkthrough / Doc Review / CRV (Quarterly moved out, replaced by Safety Check); CRV card now labeled "★ Best Value — Save $500"; JSON-LD hasOfferCatalog updated with 3 new offers at correct prices. **Services page** — new FIND / BUILD / MAINTAIN operating-model band inserted after authority statement, listing all 8 offerings with prices + BEST VALUE badge on CRV; existing anchors (`#walkthrough`, `#docs-review`, `#crv`, `#control-system`, `#incident`, `#annual`) preserved; jump-nav now includes "Corrective Action" chip; approved savings-language paragraph rendered verbatim below FBM grid; Doc Review card copy rewritten to reflect approved baseline scope (one facility, up to 5 categories, up to 25 files, one findings-review call); CRV price card + "Value comparison" band updated to $2,500/$3,000/$500; REACT_APP_GL_WEB_008_ENABLED feature flag retired. **CRV detail page** — title, meta, JSON-LD, hero H1, price display, related-service card all rebased to $2,500. **CitationProofKitDetailPage** — new "Need Help Implementing the System?" bridge inserted between the pricing tiers and the physical-mockup section on all 5 kit detail pages, CTA routes to `/services/corrective-action-implementation`. **ClientIntakePage** — new dropdown mapping `'corrective-action-implementation' → 'not_sure'`; existing labels updated to $1,300 / $2,500. **CityLandingPage + SSR generator** — 11 city seoDescs updated; `priceStart` default 1200 → 1300; `priceRangeTop` 2000 → 2100 so the walkthrough range is $1,300–$2,100 not colliding with the new $2,500 CRV. **SSR generator** — mass replaced $1,200 → $1,300 and $2,000 → $2,500 across all city + service SSR routes; Document Development pricing table restored to $1,200/$2,000 (unrelated to walkthrough/CRV); new SERVICE_DETAIL_ROUTES entry for Corrective Action Implementation with breadcrumb; sitemap.xml adds `/services/corrective-action-implementation`. **All service prices consistent** — final grep sweep confirmed 0 stale walkthrough/CRV prices remain. Remaining `$1,200`/`$2,000` occurrences are legitimate Document Development pricing (unchanged per brief) or field-note narrative cost references (arc flash / respirator program / crane inspection). **File inventory**: 21 files modified, 2 files created (`servicePricing.js` + `CorrectiveActionImplementationPage.js`). 0 backend/checkout changes. Lint clean on all touched files. **NOT DEPLOYED** — awaiting explicit preview review and approval before Vercel push.
- 2026-02 — **Automated PDF delivery for Citation-Proof Kit Series ($150 Digital)**: Swapped from manual fulfillment to auto-attach. `libreoffice-core` + `libreoffice-writer` installed system-wide. New builder script `/app/scripts/build_citation_proof_kit_pdfs.py` downloads the source DOCX from artifact URLs, renders to PDF via `libreoffice --headless`, then prepends the GigLine v3 branded cover via `lib/pdf_cover.py` — producing `GigLine_LOTO_Digital_Compliance_Kit_150.pdf` (27pp, 560KB) and `GigLine_PIT_Digital_Compliance_Kit_150.pdf` (20pp, 320KB) in `/app/backend/kit_files/`. `citation_proof_kits.py`: `_send_buyer_confirmation()` now base64-attaches the correct branded PDF and returns bool; on success email copy is "Your kit is attached to this email" + "How to use this kit" (open PDF → Citation-Proof Score™ Rubric → run Builder tool → reply for facility-specific Q&A); on missing-file falls back to old "delivered separately" copy so no buyer is silently stranded. `_send_vince_notification()` now branches on `pdf_delivered`: success = green-callout FYI (no action needed); failure = amber ACTION REQUIRED. DB `fulfillment_status` written as `auto_delivered` or `pending_manual` after the send. Config: `CITATION_PROOF_KIT_DIGITAL_PRODUCTS` now includes `pdf_path` + `pdf_filename` per kit. Thank-you page copy updated to match: "Check your inbox → Your kit was just emailed to you" + revised "What happens next" list. Two new pytest tests verify the PDFs exist on disk with reasonable size — 8 tests total, all pass. Rebuild PDFs anytime with `python /app/scripts/build_citation_proof_kit_pdfs.py`.
- 2026-02 — **Kit page conversion pass (all 5 kits)**: Unified CTAs and added conversion-focused sections. (1) Hero primary CTA on all detail pages → "Choose Your {Kit} Kit" (LOTO/PIT/HazCom/Incident/New Hire) — scrolls to `#pricing` instead of driving direct-to-purchase. Digital-tier CTA on the pricing card retains the tier-specific "Buy Digital Kit — $150" label per spec item 8. Not-ready kits still show "Notify Me When Available". (2) Hero secondary CTA → "Compare the 3 Options" — scrolls to `#pricing`. (3) $300 tier badge → "Recommended for most facilities" (was "Most Popular"); reduced letter-spacing + font-size to fit the longer string; `isFeatured` check now keyed on `tier.id === 'control-system'` instead of the badge string. (4) New per-kit `heroSupportLine` field renders directly under H1 headline — kit-specific role/outcome copy. (5) New per-kit `builtFor` array (3 role/description items) rendered in a new "Built For" white section immediately before pricing — with gold check icons on beige tiles. Data changes in `citationProofKits.js`, layout in `CitationProofKitDetailPage.js`. Structure/order and tier count unchanged.
- 2026-02 — **PIT detail page product images wired**: 5 content-rich PNG compositions saved as PNG per user rule (no WebP, no resize/crop/borders) into `/app/frontend/public/assets/kits/pit/` — `pit-hero.png`, `pit-problem-panel.png` (converted from source JPG), `pit-whats-inside.png`, `pit-matrix.png`, `pit-physical-mockup.png`. `citationProofKits.js` PIT entry gets a `productImages` block matching LOTO's structure; catalog `cardImage` updated so the PIT card in the grid now shows the full cover art. LOTO-specific alt text hard-coded in `CitationProofKitDetailPage.js` refactored into per-kit `productImages.problemPanelAlt` / `productImages.photoMapSpotlightAlt` overrides (LOTO alt behavior unchanged via fallback). PIT alt text now correctly describes the Training & Evaluation Builder™ for accessibility + SEO.
- 2026-02 — **Citation-Proof Kit Series — $150 Digital Stripe checkout wired + PIT page deepened**: (1) New backend route `/api/checkout/citation-proof-kit-digital` (POST, body: `{slug, origin_url, email?, company_name?, attribution?}`) creates Stripe Checkout sessions for LOTO ($150) and PIT ($150) Digital tiers only. Slug allowlist enforced — HazCom/Incident/New Hire kits return 400. `/api/citation-proof-kit/verify` (GET `?session_id=`) is polled from the thank-you page; on first paid verify it sends the tier-specific buyer confirmation via Resend and an ACTION REQUIRED notification to Vince. Payment metadata + orders stored in Mongo collection `gl_citation_proof_kit_orders`. Backend module: `/app/backend/routes/citation_proof_kits.py`. Config: `CITATION_PROOF_KIT_DIGITAL_PRODUCTS` dict in `config.py`. Buyer enrolled in MailerLite Lead Nurture via fire-and-forget task. (2) `KitPricingTiers` component swaps the Digital-tier CTA from `<Link>` to `<button>` for LOTO + PIT slugs — clicking calls the checkout endpoint and redirects to Stripe. $300 Control System and $600 Binder Edition tiers still route to `/contact` per spec. (3) New page `CitationProofKitThankYouPage.js` at `/citation-proof-kits/:slug/thank-you` polls verify every 1.5s (max 6 tries). (4) PIT detail page copy DEEPENED using the uploaded `GigLine_PIT_Digital_Compliance_Kit_150.docx` — added `stakes` (OSHA #6 most-cited, 2,248 FY2024 citations, three stat cards with $16,550 / $165,514 / 2,248), 4 outcomes cards, 4 `controlMechanisms` entries, 5 FAQ entries, 9-item `keyProof` list. All copy sourced verbatim from the DOCX. (5) Regression pytest at `/app/backend/tests/test_citation_proof_kits.py`. Route registered in `/app/backend/server.py`. Thank-you route registered in `/app/frontend/src/App.js` (BEFORE the `:slug` catchall).
- 2026-02 — **"Visibility problem" mindset-shift copy propagated across 5 touchpoints**: Rewrote key sales messaging around the aha: *"If it isn't assigned, verified, and documented, it isn't controlled."* (1) `/supervisor-kit` Problem section H2 changed to *"They don't just have a paperwork problem. They have a visibility problem."* with new payoff paragraphs (*"Most safety gaps do not start as major failures — they start as things someone noticed, mentioned, meant to fix, and never documented all the way through"* + *"The Supervisor Safety OS turns those loose ends into a visible system: inspect, document, assign, verify, and review"*); (2) `/supervisor-kit` Outcome section gained a gold-border pull callout: *"Safety documentation is not about having forms — it is about proving the work is being followed through"*; (3) meta + OG descriptions updated on both React SEO tag AND SSR pre-render (`generate-seo-pages.js`) to the new "visible supervisor-run system" framing; (4) `/thank-you-intake` gained a gold-border pull-quote below the "Three steps" block using the "loose ends" line + payoff *"That's what this engagement is here to close"*; (5) `pdf_cover_metadata.py` (`gl-doc-review-prep-checklist.pdf` body) rewritten to lead with *"Safety documentation is not about having forms — it is about proving the work is being followed through. Pull these records together so we can score what you already have and show you exactly where the visibility gaps are"* — regenerated a personalized cover and verified via `fitz` text extract that the new body appears on page 1 of the "PREPARED FOR {COMPANY}" personalization; (6) intake confirmation email (`routes/intake.py` `client_html`) gained a gold-border pull-quote HTML `<table>` between the "3 steps" `<ol>` and the Field Manual CTA, mirroring the same aha before the recipient even opens the PDF. Zero design, pricing, checkout, product-structure, or CFR/citation-language changes.
- 2026-02 — **Homepage "Latest from the Field" strip shipped**: New section `data-testid="latest-from-field-section"` inserted between the How It Works and About Vince sections on `HomePage.js`. Kicker *"Latest From the Field"* (gold `#C9A84C`) · H2 *"What Vince is seeing on Piedmont Triad floors right now"* · 4-card grid (1 Blog Post highlighted with gold kicker + 3 Field Notes with muted mono kicker): (a) **Blog** — Mid-Year 2026 OSHA Update for NC Manufacturers, (b) **Field Note** — AI-Generated Safety Programs, (c) **Field Note** — Heat Stress, (d) **Field Note** — Forklift Safety. "Browse all Field Notes" underlined CTA below the grid. Uses same visual language as `RelatedFieldNotesStrip` but supports mixed card types (Field Note vs Blog Post). Fires no analytics — just link equity + freshness signal for the homepage. All 4 links validated to correct routes via automated screenshot script.
- 2026-02 — **3 service-area landing pages registered in SSR** (`generate-seo-pages.js`):
  - `/forklift-compliance-review-nc` — Service schema (price $1,200, 29 CFR 1910.178), 4 FAQ entries, BreadcrumbList, ~250 words of crawler-readable content
  - `/loto-procedure-review-nc` — Service schema (price $1,200, 29 CFR 1910.147), 4 FAQ entries, BreadcrumbList, ~250 words of content
  - `/osha-documentation-review-nc` — Service schema (price $1,300, OSHA documentation review), 4 FAQ entries, BreadcrumbList, ~250 words of content
  - Each page now ships pre-rendered HTML for crawlers (Google reads JSON-LD without rendering JS), and Helmet still hydrates the same tags client-side. SSR build now generates 71 SEO HTML files (was 68).
- 2026-02 — **Vercel build fix** — `commitNavigation` in `SiteSearch.js` wrapped in `useCallback` to satisfy CRA's `react-hooks/exhaustive-deps` rule. Verified via testing agent + local `CI=true yarn build` (21.67s, exit 0).
  - **Admin search bar** at top of toolbar — case-insensitive filter across Intakes (company, contactName, email, phone, clientToken, dba, industry), Bookings (company, contact, email, phone, clientToken, service), Safety Check submissions (name, email, company), and Walkthrough Leads CRM (via `externalQuery` prop). Verified: filtering "test" on 11 intakes → 4 (the test leads). Clear button (X) inline.
  - **Public-site search** — new `<SiteSearch>` modal triggered by search icon in Navbar (desktop + mobile). Keyboard shortcuts: `/` or `Cmd/Ctrl+K` to open, arrow keys + Enter to navigate, Esc to close. 71-entry hand-curated index in `src/data/siteSearchIndex.js` covering all pages, 25 Field Notes, 7 services, 3 service-area landings, 5 lead magnets, case study, blogs, FAQ, resources. Score-weighted ranking (title-start = 100, title-contains = 70, keywords = 40, type = 20).
- 2026-02 — **🎨 Color refinement: deep dark navy `#102A43` for section backgrounds, bright `#2A52A0` for headers/CTAs** — Per user palette doc + screenshots, refined the rebrand:
  - Section/hero backgrounds (HomePage hero, Trust band, About, Case Study, FAQ navy sections, Field Notes hero, Services Founder band, all blog navy sections) → `#102A43` (deep dark navy, almost black-blue)
  - UI chrome (admin table headers, drawer headers, filter pills, navbar CTA, button backgrounds) → `#2A52A0` (bright royal navy)
  - SampleReportSection report header → `#102A43`
  - Excluded `AdminPage.js` + `WalkthroughLeadsCRM.js` from the bg-deep-navy sweep so admin tables stay bright navy.
- 2026-02 — **Brand rebrand: Navy is now Primary Dark (sitewide)** — Initial sweep: 134+ instances of `#1C2B2B` (deep teal/charcoal — read as "green" on some displays) swapped to navy across 20+ files.
- 2026-02 — **Inline delete buttons added to Admin lead tables** — One-click archive (recoverable) of test/junk leads, with Shift+Click for hard-delete. Backend: `LEAD_COLLECTIONS` now supports a `fallback_id_field` (e.g., intake falls back from `clientToken` → `id`), so legacy test leads created before `clientToken` was introduced are also deletable. Frontend: red `<Trash2>` icon button added to Intakes table row (✓ 11 buttons rendering) and Safety Check submissions row (✓ 9 buttons rendering). Walkthrough Leads CRM already had inline delete. Archived leads land in `<collection>_archive` for recovery.
- 2026-02 — **Admin "Sources — Last 30 Days" tile added** — Closes the loop on the review-CTA effort. New grid of compact stat cards (5-col desktop / 2-col mobile) above the existing all-time "Conversions by Source" table showing first-touch UTM source counts for last 30 days. Pulls from BOTH `walkthrough_requests` (top-level `utm_source`) AND `gl_intake_submissions` (`attribution.firstTouch.utm_source`). Review-CTA surfaces (`case-study`, `thankyou-intake`, `footer`) highlighted in gold. Backend: new `lead_sources_30d` field in `/api/admin/stats`. Frontend tile at `data-testid="lead-sources-30d"`.
- 2026-02 — **Google review CTAs added** to 2 high-intent pages:
  - **Case Study (`/case-study/metals-fabrication-statesville`)**: New "Worked with GigLine? Leave a Google review" strip below the closing CTA band. UTM: `?utm_source=case-study&utm_medium=website&utm_campaign=review-request`. Uses gold-on-navy outline pattern, side-by-side at desktop, stacks at mobile.
  - **Intake Thank-You (`/thank-you-intake`)**: New card between CTA cards and footer with "Already worked with GigLine before?" copy. UTM: `?utm_source=thankyou-intake&utm_medium=website&utm_campaign=review-request`. Soft-ask design — won't pressure new intakes, primes repeat clients who land here mid-engagement.
  - Goal: accelerate path to 5+ reviews so `aggregateRating` schema can be activated (unlocks rich-result star ratings in Google SERP).
- 2026-02 — **Wave 3 SEO enrichment shipped** (NC-local visibility boost):
  - **hreflang tags**: Added `<link rel="alternate" hreflang="en-us">` + `<link rel="alternate" hreflang="x-default">` to every page (via `SEO.js` for client-side + `generate-seo-pages.js` for SSR/crawlers). Self-referencing, signals US-English language/region to Google.
  - **LocalBusiness schema enrichment**: Added `slogan` ("Find the gaps before OSHA does."), `paymentAccepted` ("Credit Card, ACH, Check, Stripe"), `currenciesAccepted` ("USD"), and an 18-entry `knowsAbout` array covering OSHA standards (1910, 1926), hazard topics (HazCom, LOTO, machine guarding, fall protection, respiratory, forklift), recordkeeping, and audience verticals (manufacturing/warehouse/fleet/contractor). Strengthens topical authority signals for "OSHA consultant near me" type queries.
- 2026-02 — **Wave 2 audit fix shipped** (canonical hygiene + 301 redirects):
  - **3-way duplicate consolidated**: `/services/safety-walkthrough` + `/services/safety-walkthrough-report` → 301 to `/safety-walkthrough`. Canonical on the page updated from `/services/safety-walkthrough` to `/safety-walkthrough` (matches sitemap). BreadcrumbList schema updated.
  - **Privacy duplicate fixed**: `/privacy` → 301 to `/privacy-policy`. `PrivacyPolicyPage` canonical updated to `/privacy-policy`. Two dangling `href="/privacy"` refs in `ClientIntakePage.js` updated.
  - **Terms duplicate fixed**: `/terms` → 301 to `/terms-of-service`.
  - **HTTP-level 301s on Vercel**: Added 8 `redirects` rules to `vercel.json` (legacy `/services/safety-walkthrough*`, `/privacy`, `/terms`, `/request-walkthrough`, `/onboarding`, `/onboarding/confirmed`, `/case-studies/mocksville-plastics-osha-inspection`). React `<Navigate>` remains as belt-and-suspenders fallback. Google now sees true 301s, transfers PageRank cleanly.
  - **Aliases kept** (verified unique content): `/walkthrough` (booking landing page), `/documentation-gap-check` (Documentation Readiness Review service), `/osha-compliance-gap-check` (Compliance Readiness Visit service). All have distinct keyword-intent content and canonical URLs.
  - Verified: 6 redirects ✓, 3 canonicals ✓.
- 2026-02 — **Wave 1 audit fix shipped** (image optimization + dedupe SEO meta tags):
  - **Image weight reduction: 24.1 MB → 2.5 MB (89%, ~21.5 MB saved sitewide)**. Optimized 13 oversized images: 9 PNG photos converted to JPG (max 1600px, quality 82) + 2 graphics PNGs palette-quantized + 2 JPGs re-encoded. Per-page weight dropped 57-64% on heavy pages (forklift Field Note 3.5MB → 1.3MB, About 2.9MB → 1.3MB, city pages 3.0MB → 1.2MB). Updated 5 file references across FieldNoteDetailPage, FieldNotesPage, HomePage, AboutPage, ClientIntakePage, SSR script.
  - **Removed duplicate SEO meta tags** — Stripped static per-page meta tags (`description`, `canonical`, `og:url`/`title`/`description`, `twitter:title`/`description`) from `public/index.html` so `react-helmet-async` is the single source of truth client-side. Updated `generate-seo-pages.js` to INJECT these tags into static HTML at build time (instead of `.replace()` against now-absent template tags). Verified: every page now serves exactly ONE canonical, description, og:url tag.
  - **Page audit results**: 22 routes tested at mobile (390×844) + tablet (768×1024) → **0 horizontal overflow**, single H1 per page, 100% alt-tag coverage, all routes 200 OK, NotFound page correctly noindex'd.
- 2026-02 — **Sitewide audit copy sweep shipped** (P0 from user audit punch list, mirrored to `generate-seo-pages.js`):
  - "The same eyes an inspector uses" → "An OSHA-informed floor review before an inspector shows up"
  - "what an inspector is likely to find" → "conditions an inspector may review"
  - "penalty exposure per finding" → "estimated penalty exposure based on OSHA published maximums" (sitewide replace_all across HomePage, ClientIntakePage, WalkthroughDaySection, ServicesPage, ServiceDetailPage, SampleReportPage, OshaInspectionGuidePage, CityLandingPage, generate-seo-pages.js)
  - All "$16,550 / $165,514 / $14,502" dollar refs prefixed with "Up to " (FieldNoteDetailPage, BlogHazComRequirements, BlogOSHAViolations penaltyData tables, SampleReportSection findings)
  - "$15,621" residual on `/services` Sample Report finding 3 normalized to "Up to $16,550 (Serious)"
  - "24–48 hours / 24-48 hours / 24 to 48 hours" → "within 48 hours" everywhere (FAQ, City pages, Services, ServiceDetail, SSR script)
  - Added educational disclaimer paragraph below Case Study findings (`data-testid="case-penalty-disclaimer"`) clarifying penalty figures are estimates based on OSHA published maximums per 29 CFR 1903.15
  - Case Study finding column header renamed "Penalty Exposure" → "Estimated Penalty Exposure"
- 2026-02 — **New favicon shipped** — Black/silver-"g"/gold-buckle brand mark replaces previous icon. Generated all required sizes (16/32/48/180/192/512 PNG + multi-res `favicon.ico`) and bumped cache-buster `?v=4` in `index.html` so browsers pick up immediately.
- 2026-02 — **Audit batch 2 shipped (d/e/f/g/h):** (d) `/about` got 3 new sections — Who I Help, Why the Name GigLine (Navy origin), Service Area with 12-city pill grid; (e) `/faq` got 7 new entries (legal risk, supervisor presence, post-incident timing, insurance loss control, monthly support, etc.); (f) `/safety-walkthrough` got "Best Fit / Not Best Fit" buyer self-qualification block; (g) 3 new service-specific landing pages created via reusable `<ServiceLandingPage>` component — `/forklift-compliance-review-nc`, `/loto-procedure-review-nc`, `/osha-documentation-review-nc` — each with Schema.org Service JSON-LD, sitemap.xml entries; (h) Case Study page gained a top inline CTA above the body.
- 2026-02 — **Audit response batch shipped**: (1) Brand hex aligned site-wide to canonical `#1C2B2B` / `#2A52A0` / `#C9A84C` (1,187 replacements across 45 files); (2) Case study penalty ranges normalized to 2026 OSHA max ($16,550); (3) Blog penalty/date notes updated (Top 5 OSHA + HazCom) — "January 2025" → "2026 max effective Jan 15, 2026"; (4) Homepage hero microcopy "Private engagement. Report in 48 hours" → "Private findings. Written report within 48 hours"; (5) Homepage case study teaser updated to use 2026 max + "four days" closure; (6) Safety Walkthrough page gained "What a GigLine walkthrough is — and what it isn't" Does/Doesn't scope block + legal disclaimer; (7) Sample Report page got an ungated "Open the PDF directly" escape hatch above the email gate (lead capture preserved as the secondary option); (8) Kevin Stutts pull-quote TODO removed (quote verified).
- 2026-02 — `/contact` micro-form swap: replaced heavy ContactForm with QuickContactCard (already wired to `/api/quick-contact/submit`) + "Use the full intake form →" fallback link. Reduces friction for visitors who just want to start a conversation.
- 2026-02 — Pillar page trust strip on `/osha-compliance-guide` — 3-stat navy/gold bar above the closing CTA ("25+ Years · 12 of 13 · NC-Based").
- 2026-02 — `admin.py` refactor: extracted repetitive lead-magnet stats querying into `lead_magnet_stats()` helper. 60+ lines deduplicated. Endpoint shape unchanged, all values verified identical via curl.
- 2026-02 — Strengthened `/about` with "For Editors & Publications" press-bio block: 5 credential pills + 3 copy-paste-ready blocks (one-liner, 90-word bio, direct contact) with working copy-to-clipboard buttons. Added `sameAs` array to Person JSON-LD (giglinecompliance.com / /about / /osha-compliance-guide) so Google connects citations across the site.
- 2026-02 — **GL-WEB-021 shipped**: Compliance Readiness priceLine fixed to "From $2,000 · range $2,000–$2,400"; `/contact` Quick-Action CTA block added (Call / Email / Request Walkthrough buttons + intro copy); Safety Check tier copy updated per spec — low tier now routes to `/osha-inspection-guide`, all tiers include "Want Vince to review your score?" mailto footer; sitewide noscript fallback rewritten as GigLine pitch line.
- 2026-02 — **GL-WEB-020 shipped**: Homepage "Recent Engagement" compact proof block added between "What We Find" and "Why GigLine" (navy + gold accent linking to case study); Case Study page metrics bar relabeled ("To Close 12 of 13"); 13-row Corrective Actions table + Outcome block + pull quote live; all "findings closed" copy updated to "corrective actions closed within four days of the walkthrough" across homepage, schema, and SSR mirrors.
- 2026-02 — **`/supervisor-kit` full page rewrite → outcome-based positioning + 3-tier pricing**: Sales page rewritten per Vince's launch spec. New sections: **Problem** ("Safety work often happens. The proof does not."), **Outcome** (before/after grid), **Rhythm** (Inspect → Document → Assign → Verify → Review visual chain), **What's Included** (17-doc table with Code / Document / What It Helps You Do columns), **Value Framing** (4 cards: Clarity / Control / Proof / Rhythm + pull quote), **Pricing** (3 cards), **Who This Is For**, **Important Notice** (legal/expectation language: "does not guarantee OSHA compliance, prevent citations, eliminate hazards…"), **Final CTA**. **New 3rd tier: OS Implementation Review — from $850** (mailto CTA with pre-filled subject/body since it needs scoping — no Stripe SKU). **Stats bar updated** to `17 Documents / 20-Page System / 16 Inspection Items / OSHA-Aligned Field Tools` (dropped $16,550 penalty). **All "citation-proof / prepared for any inspection / Every form maps to an OSHA standard" language removed** from the sales page. **Digital fulfillment simplified**: SUPERVISOR_KIT_FILES now attaches ONLY the complete `GigLine_Supervisor_Safety_OS_v1.pdf` (the 17 individual per-doc PDFs stay on disk under `SUPERVISOR_KIT_INDIVIDUAL_FILES` for future v1.1 launch). Digital email copy rewritten to outcome-based framing with the Important Notice callout inline. **Vince notification email upgraded**: now includes buyer **phone** (Stripe captures it for physical orders), shipping block, updated fulfillment notes ("system auto-emails" for digital, "USPS Priority (free — no charge to buyer)" for physical). **Product name consistency**: standardized to "GigLine Supervisor Safety OS", "Digital Kit", "Physical Binder Kit", "OS Implementation Review" across frontend + Stripe product names + emails + SSR + blog cross-sells (renamed "Get the Supervisor Kit" → "Get the Supervisor Safety OS", "Physical Kit" → "Physical Binder Kit").
- 2026-02 — **GigLine Supervisor Safety OS v1 PDFs wired**: Vince uploaded the complete 20-page master `GigLine_Supervisor_Safety_OS_v1.pdf` (587 KB, correct structure — cover, notice, TOC, and one page per SS-XX document in the exact order that matches the sales page). Saved to `/app/backend/kit_files/`. Built idempotent `scripts/split_supervisor_os.py` that extracts 17 individual per-document PDFs (SS-01…SS-12 with A/B variants for 03/04/06/07/09) into `kit_files/os_v1_individual/` — each keeps its own page, ready for reprints. `config.py` `SUPERVISOR_KIT_FILES` refactored: now attaches the complete master PDF **plus** all 17 individual files (18 attachments total, ~657 KB — well under Resend's 40 MB limit). Digital-fulfillment email rewritten to reference the new file layout (opens with the complete PDF as "the bound system with cover, notice, TOC, and every document", then explains individual SS-XX files for reprinting). Fixed a "GigLine GigLine" duplication bug introduced by the earlier rebrand sed (email subject, FieldNoteDetailPage cross-sell copy, SupervisorKitThankYouPage SEO description, and 3 field-note kit intros). Legacy 11 v0 kit PDFs archived to `kit_files/archive_v0/` for safe rollback.
- 2026-02 — **Supervisor Kit rebranded → "GigLine Supervisor Safety OS" + product update**: Rebrand swept sitewide (32 replacements across 20+ files: sales page, HomePage, ServicesPage, ResourcesPage, Footer, AdminPage, 3 blog posts, FieldNoteDetailPage, SupervisorKitThankYouPage, `fieldNoteContent.js`, 4 backend routes, 3 leave-behind scripts, mailerlite integration, generate-seo-pages SSR, and PDF `/Subject` metadata on all 11 legacy kit files — visible page content untouched). **Product content updated on `/supervisor-kit`**: hero kicker "SUPERVISOR SAFETY OS"; 11 → **17 documents** with new SS-01…SS-12 codes (including A/B example/blank variants for 03, 04, 06, 07, 09); stats bar recalculated (17 Documents / 16 Inspection Items); value callout now cites **$800–$1,200** consultant benchmark; digital bullets mention the complete 20-page system + individual files; physical bullets highlight **free USPS Priority shipping**. **Pricing**: Physical Kit **$675 → $700** in `config.py` (`amount_cents=70000`), pricing card + CTA + subline updated. **Free shipping**: `stripe_native.py` now injects an explicit `shipping_options` array with a $0 "Free USPS Priority Shipping" rate (2–5 business day delivery estimate) so the Stripe Checkout page shows "Free" instead of leaving shipping ambiguous; address still captured for fulfillment. **SEO/SSR mirrored**: SSR body for `/supervisor-kit` regenerated with the 17-doc list and $700 free-shipping copy. **Verified**: `cs_live_...` Stripe session created successfully with new price + free-shipping option; feature flag still guards the routes (503 with new brand name in the message); flags restored to soft-disabled state.
- 2026-02 — **"Veteran-Owned Company" trust badge deployed sitewide (6 placements + brand recolor)**: Recolored the raw royal-blue source (`RGB(0,32,96)`) → GigLine navy `#102A43` via exact-match + anti-aliased edge remap in Pillow; flag / white stars / white borders preserved. Saved as `/assets/veteran-owned-badge.webp` (26KB), `veteran-owned-badge.png` (34KB), and small variants (`-sm.webp` 9KB / `-sm.png` 23KB) for footer/PDF/email use. **Placements shipped:** (1) Footer trust strip (below "Kernersville, NC"), (2) Homepage hero corner stamp on Vince's photo (bottom-left, 110px), (3) Contact page next to availability block, (4) Case Study "About the Assessor" byline (new section between CTA and review strip), (5) Thank You / Intake success page above the "questions before we follow up?" footer, (6) All 8 collateral PDF covers via new `pdf_cover.py` bottom-left placement, AND (7) email signature block in every transactional email template (intake, contact-message, sample-report, heat-guide, hazcom, osha-inspection-guide, supervisor-kit — 7 templates). Also fixed a preexisting PDF metadata bug that was causing duplicate covers on 5 of 8 PDFs (`gl-faq`, `gl-pricing`, `gl-osha-inspection-guide`, `gl-case-study-statesville`, `gl-lm-001-digital` all had `replace_page_1=False` despite already having v3 covers as page 1) — flipped all 8 to `True`, stripped the 5 duplicate page-2s. All PDFs now: 1 cover (with badge) + content. Copy correction: "U.S. Navy Veteran" used consistently (matches existing hero trust badge).
- 2026-02 — **GL-WEB-026 shipped — Field Notes SSR fix**: Extracted the 25-note `NOTES` content database from `pages/FieldNoteDetailPage.js` into a shared CommonJS-compatible file `src/data/fieldNoteContent.js` (1,414 lines). React page now `import`s from it (no visual regression). `scripts/generate-seo-pages.js` `require()`s the same source of truth and calls a new `renderFieldNoteBody()` helper that emits semantic HTML for every note (h1 + subtitle + CFR reference + What this is / What gets missed / What OSHA checks / What I see on the floor / Field checklist + full FAQ block + FAQPage JSON-LD). Before: 23 of 25 notes shipped ~200 chars of SSR body; after: 2,300–8,500 chars per note (10–40× content). Verified end-to-end by running the generator against a mock build template: `heat-stress/index.html` went from 3.4KB (empty body) → 10.2KB (6,475-char article body, 1 h1, 7 h2s, 24 `<li>`s, FAQ schema present). Custom-content notes (e.g. `ai-generated-safety-programs`) untouched via the existing `note.customContent` short-circuit.
- 2026-02 — **Google Indexing API wired (service-account OAuth)**: New `POST /api/admin/google-index/submit-url` (single URL, URL_UPDATED or URL_DELETED), `POST /api/admin/google-index/submit-sitemap` (bulk push from `sitemap.xml` filtered by `<lastmod>` days, hard-capped at 100/call to preserve daily quota), `GET /api/admin/google-index/status` (config health + service-account email + sitemap presence), `GET /api/admin/google-index/log`. All submissions logged to Mongo `google_indexing_log`. New Admin dashboard tab **SEO Indexing** with three cards (status / single / bulk) + recent-submissions table. Credentials loaded from base64-encoded env var `GOOGLE_INDEXING_SA_JSON_BASE64`. Full one-time setup guide written to `/app/memory/GOOGLE_INDEXING_SETUP.md` (GCP project, enable Indexing API, service account, JSON key, add as Owner in Search Console, base64-encode & drop in .env, restart). Endpoints validated for 401 (bad token), 400 (bad URL), 503 (missing creds), and 200 with a mocked (valid-shape) SA JSON.
- 2026-02 — **Personalized Doc Review Prep Checklist wired into intake confirmation email**: When a client submits an intake with `serviceSelected="doc_review"`, the confirmation email now attaches a per-client copy of `gl-doc-review-prep-checklist.pdf` with a "PREPARED FOR {COMPANY} | 2026" cover generated via `lib/pdf_cover.prepend_cover_to_pdf` (same v3 template used by the Admin "Personalize PDF" tab). Filename: `GigLine_DocReview_PrepChecklist__{slug}.pdf`. Falls back gracefully to the static PDF if personalization fails. Also fixed a preexisting duplicate-cover issue in the base PDF (was 4 pages: 3 stacked covers + 1 content) and updated `pdf_cover_metadata.COVERS['gl-doc-review-prep-checklist.pdf'].replace_page_1` to `True` so future apply-cover re-runs swap instead of stack.
- 2026-02 — **"12 of 13 Corrective Actions" social-proof graphic wired** — Full-width outcome banner (`/assets/case-study-statesville-12-of-13.webp`) inserted on `/services/compliance-readiness-visit` between hero and What-You-Get block (with "Real outcome from a 2026 Compliance Readiness Visit — Statesville, NC · View the case study →" caption); smaller inline stat card added to `/walkthrough` landing page between "what happens next" and the form (attribution "From a Compliance Readiness Visit (walkthrough + doc review combined) — Statesville, NC" + CTA to full case study). Both link to `/case-study/metals-fabrication-statesville`.
- 2026-02 — Fixed Navbar tagline from "Greensboro, NC" → "Kernersville, NC" (was the last stale location reference sitewide).
- 2026-02 — Wired **8 real walkthrough photos** as hero images on Field Note detail pages: HazCom, Electrical Safety, Lockout/Tagout, Forklift Safety, Walking Surfaces, Emergency Action Plans, Recordkeeping & 300 Log, Machine Guarding. Each photo also flows into the page's `og:image` meta + Article schema `image` (rich social previews + Google rich results per topic). Total: 9 of 25 Field Notes now have hero photos.
- 2026-02 — Fixed broken machine-guarding photo in Sample Report Section on `/services` — saved real photo to `/floor-findings/machine-guarding.jpg`.
- 2026-02 — Built **OSHA Compliance Guide pillar page** at `/osha-compliance-guide`. Topical index of all 25 Field Notes organized into 6 hazard clusters (Chemical & Health, Mechanical & Energy, Fall & Height, PPE, Process & Recordkeeping, Operations). CollectionPage + WebPage + BreadcrumbList schema. Added to /resources hub, sitemap.xml, and SSR pre-render with full content + 25 internal links. Removed retired `/onboarding` URL from sitemap.
- 2026-02 — Expanded "Related Field Notes" cross-linking from 2 → 3 per note across all 25 Field Notes (React side). Mirrored into `generate-seo-pages.js` SSR pre-render so crawlers see the internal links without JS — topic-cluster signal for Google.
- 2026-02 — Upgraded Article JSON-LD schema on all 25 Field Notes + 2 inline blog articles + 1 case study (added `image`, `datePublished`, `dateModified`, `articleSection`, `inLanguage` — Google rich-result eligible). Corrected 3 inline schemas that had future `datePublished` dates.
- 2026-02 — Fixed Admin Dashboard HR tile (`hr_osha_guide` dict added to `/api/admin/stats` response).
- 2026-02 — Added Supervisor Safety Starter System tile to `/resources` page + mirrored in `generate-seo-pages.js`.
- 2026-02 (prev fork) — GL-WEB-020: Auto-open PDF on all gated downloads.
- 2026-02 (prev fork) — GL-WEB-019: HR-facing copy + new `/osha-inspection-guide` page.
- 2026-02 (prev fork) — Sample Report swapped to real redacted PDF.
- 2026-02 (prev fork) — Legacy `/onboarding` retired.
- 2026-02 (prev fork) — Factual sweep + Google Review UTM consolidation.
- 2026-02 (prev fork) — 9 new Field Notes (25 total) + backlink playbook.
- **2026-02 — Product Discovery Update (Phase 1 + Phase 2)**:
  - `/services` MAINTAIN column expanded from 3 → 5 items in exact order: Citation-Proof Kit Series ($150/$300/$600) → Supervisor Safety OS (From $600, `/supervisor-kit`) → HazCom Starter Pack ($29, `/hazcom-starter-pack`) → Quarterly Compliance Maintenance (From $950/quarter) → Annual Compliance Control Partner ($12,000/year). Existing card component/pattern reused; no visual redesign. `ServicesPage.js` lines ~495–500.
  - Homepage: new **Secondary Product Discovery Strip** inserted immediately below the Floor→Findings→Fixes→Proof section (before "What we find on the floor"). Heading "Build the control your team can run.", supporting copy "GigLine tools help operations turn safety expectations into assigned ownership, usable records, corrective-action follow-up, and retrievable proof." 3 lightweight cards: Citation-Proof Kit Series ($150–$600, `/citation-proof-kits`), Supervisor Safety OS (From $600, `/supervisor-kit`), HazCom Starter Pack ($29, `/hazcom-starter-pack`). Warm cream `#f5f4f0` background, gold top-rule, muted CTAs — intentionally quieter than the primary CRV CTA and the dark-navy Citation-Proof Kit Series band further down. `data-testid="home-product-discovery-strip"` and per-card CTAs. Includes disclaimer footnote that tools do not replace Walkthroughs / Documentation Readiness Review / CRV. `HomePage.js` inserted after buyer-journey (line ~395).
  - Supervisor Safety OS pricing confirmed on existing product page: $600 digital / $700 physical binder → per spec, displayed as "From $600" everywhere.
  - No navbar changes. No SEO route/metadata changes. No changes to Safety Walkthrough / Doc Readiness Review / CRV / OSHA-Ready Control System / Corrective Action Implementation pricing.
- **2026-02 — HazCom Starter Pack hero photo**: Added real product photo (`/public/hazcom-starter-pack-hero.png`, 2.2MB) showing the yellow SDS Binder + 3 printed OSHA-compliant forms (Written HazCom Program / SDS Binder Checklist + Index / Training Verification Log) + safety glasses + gloves on a stainless workbench with GHS pictograms on the wall. Rebuilt `/hazcom-starter-pack` hero as a two-column layout (copy left, photo right on desktop, stacked on mobile) inside the existing navy `#102A43` band. All copy identical. Photo has full descriptive alt text for SEO/accessibility. Test-id: `hazcom-hero-image`.
- **2026-02 — Homepage product-discovery card thumbnails**: Replaced the initial cropped-screenshot thumbnails with **premium editorial B2B photography** generated via Gemini Nano Banana (`gemini-3.1-flash-image-preview`) using tailored prompts per product family. Final assets in `/public/`: `card-citation-proof-kits.jpg` (navy binder + tablet + checklist on wooden workbench, warehouse aisle w/ forklift blurred bg, 184KB), `card-supervisor-safety-os.jpg` (diverse operations supervisor with clipboard + tablet on manufacturing floor, 216KB), `card-hazcom-starter-pack.jpg` (yellow SDS binder + labeled chemical containers + checklist + PPE on shop workbench, 180KB). All three: **identical 1600×900 (16:9) crop**, `object-cover`, bright industrial lighting, GigLine palette (navy/gold/orange/safety-yellow accents). Design-layer branded product-label overlay (`data-testid="home-product-discovery-card-{key}-overlay"`) added in CSS with backdrop blur — 3px gold left-rule, JetBrains Mono eyebrow ("KIT SERIES" / "OPERATING SYSTEM" / "STARTER PACK") + Georgia serif product name in gold on navy 86% background. Full descriptive alt text on each `<img>`. Old PNG assets (`citation-proof-kits-card.png`, `supervisor-safety-os-card.png`) deleted from `/public/`. Cards remain visually quieter than the primary CRV CTA — the strip is complementary product discovery, not competitive.
- **2026-02 — Tablet layout cleanup (iPad 768–1023px)**:
  - **Footer** (`components/Footer.js`): Resources column now spans both columns at `sm/md` (`sm:col-span-2 lg:col-span-1`) so it becomes a clean full-width row below Company Info + Quick Links at iPad width, eliminating the orphaned narrow Resources column and the awkward right-side whitespace.
  - **Services hero** (`pages/ServicesPage.js` ~line 341): Changed image + copy split from `md:flex-row` to `lg:flex-row` — image now stacks on top full-width on iPad instead of being force-cropped into a narrow 2/5 column. Fixes the "warehouse worker crops at ankles on iPad" issue. Image height set to `md:h-80 lg:h-full` for improved iPad framing.
  - **"What's Included" / feature-card grids** on service detail pages: changed `md:grid-cols-3` → `lg:grid-cols-3` (stack fully until desktop) on `SafetyWalkthroughPage.js` (Three deliverables), `HazComPage.js` (3 doc cards), `DocumentDevelopmentPage.js` (Why This Matters + How It Works), `ComplianceReadinessVisitPage.js` (Who This Is For), `AnnualCompliancePartnerPage.js` (Who This Is For). Cards now render at full readable width on iPad.
- **2026-02 — Service page hero photos regenerated (Nano Banana editorial style)**: Generated 3 premium editorial B2B photographs via `gemini-3.1-flash-image-preview` and wired into the primary diagnostic service page heroes so the whole funnel is visually consistent with the Homepage product-discovery strip.
  - `/public/service-hero-walkthrough.jpg` (241KB) — female safety consultant in orange vest walking a real manufacturing floor with clipboard, CNC machines and forklift in view.
  - `/public/service-hero-doc-review.jpg` (210KB) — stack of navy hardcover binders with gold nameplates + color-coded tabs, printed safety-program pages with highlighter and red pen, laptop with checklist UI, coffee cup on dark wood desk, blurred shop floor through window.
  - `/public/service-hero-crv.jpg` (267KB) — diverse consultant and Hispanic facility supervisor walking a manufacturing floor together, consultant holds a tablet with a green-checkmarked checklist, forklift + racking + safety-yellow floor markings in bg.
  - Applied via two-column hero: copy left, image right at `lg:`, stacked on mobile/tablet — same rounded-xl + gold-tinted border + soft shadow treatment as HazCom hero.
  - `SafetyWalkthroughPage.js` and `ComplianceReadinessVisitPage.js` were updated inline; `DocumentationGapCheckPage.js` passes a new `heroImage` prop to the shared `ServiceLandingTemplate.js` (backward-compatible — `OshaComplianceGapCheckPage.js` remains unaffected since it doesn't pass the prop). Full descriptive alt text on all three images.
- **2026-02 — Safety Check single-flag routing threshold**: Lowered the medium-tier threshold in `SafetyCheckPage.js` so 1 flagged topic now unlocks the topic-routed kit / walkthrough recommendation instead of falling into the LOW EXPOSURE guide. New tier logic: `low` = 0 flags (all 6 confirmed), `medium` = 1–3 flags (topic-routed), `high` = 4+ flags (CRV). Medium-tier copy adapts: single-flag users see "You have a documented gap in one cited area" + a supportive body line about matching kit / walkthrough. `scoreLevel` variable updated to match.
- **2026-02 — Corrective Action Implementation hero (Nano Banana editorial)**: Regenerated `/services/corrective-action-implementation` hero to match the funnel style. New asset `/public/service-hero-cai.jpg` (234KB) — safety technician in an orange vest kneeling to install a bright yellow machine guard on a lathe with a wrench, open Corrective Actions binder and torque wrench on workbench, forklift blurred behind. Hero restructured from `grid-cols-2 (copy + pricing panel)` → two-column hero `grid-cols-[1.15fr_1fr] (copy + editorial photo)` matching Walkthrough / Doc Review / CRV / HazCom pattern; pricing panel now sits full-width below the hero (`data-testid="cai-pricing-panel"`). All copy identical.
- **2026-02 — Citation-Proof Kit detail hero photos (Nano Banana editorial)**: Replaced the 3 shipping-kit detail hero mockups with editorial photography matching the Homepage discovery cards and the 4 core service page heroes. Old branded illustration PNGs preserved on disk untouched.
  - LOTO Readiness Kit → `/assets/kits/loto/loto-editorial-hero.jpg` (210KB) — sharp close-up of red LOTO padlocks and yellow DANGER "DO NOT OPERATE / LOCKED OUT" tag on a CNC mill's isolation switch (machine ID plate: CNC MILL-04), safety-yellow floor markings and orange racking blurred behind.
  - Forklift/PIT Readiness Kit → `/assets/kits/pit/pit-editorial-hero.jpg` (272KB) — female operator in orange hi-vis vest + yellow hard hat performing a pre-shift forklift inspection with clipboard, orange pallet racking + wooden pallets + safety-yellow floor markings.
  - HazCom Pro Kit → `/assets/kits/hazcom-pro/hazcom-editorial-hero.jpg` (230KB) — organized chemical storage shelving with clearly labeled workplace chemical containers showing GHS pictograms, a yellow SDS binder, Hazard Communication Training Log clipboard, and eye-wash station in the frame. (Pro Kit product itself still blocked on Vince's DOCX upload — this is purely the marketing hero.)
  - `citationProofKits.js` data file updated to point to the new `.jpg` paths (kit detail page already conditionally renders the 2-col navy hero when `productImages.hero` is present).

## Test Credentials
See `/app/memory/test_credentials.md`.
